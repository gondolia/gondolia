# Copy this file to .env.local for local development (outside Docker).
# Not needed when using docker compose (Traefik handles API routing).

# Proxy API calls to the running Docker/Traefik gateway
API_PROXY_TARGET=http://localhost:3001

# Tenant for local testing (matches Docker seed data)
NEXT_PUBLIC_TENANT_ID=demo
